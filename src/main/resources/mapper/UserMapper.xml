<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.example.mapper.UserMapper">

    <!-- 查询所有 -->
    <select id="findAll" resultType="com.example.entity.User">
        SELECT * FROM user
    </select>

    <!-- 根据ID查询 -->
    <select id="findById" resultType="com.example.entity.User" parameterType="Integer">
        SELECT * FROM user WHERE id = #{id}
    </select>
    <!--登录-->
    <select id="login" resultType="com.example.entity.User" parameterType="String">
        SELECT * FROM user WHERE username = #{param1} and password = #{param2}
    </select>

   <!--&lt;!&ndash; &lt;!&ndash;注册&ndash;&gt;&ndash;&gt;
    <select id="register" resultType="com.example.entity.User" parameterType="User">
        insert into user (username,password) values ( #{username} , #{password},#{role})
    </select>
-->
    <!-- 创建 -->
    <insert id="create" parameterType="com.example.entity.User">
        INSERT INTO user(username, password,role) VALUES (#{username}, #{password})
    </insert>

    <!-- 删除 -->
    <delete id="delete" parameterType="Integer">
        DELETE FROM user WHERE id = #{id}
    </delete>

    <!-- 更新 -->
    <update id="update" parameterType="com.example.entity.User">
        UPDATE user SET WHERE 1=1
        <if test="username != null">
            username = #{username},
        </if>
        <if test="password != null">
            password = #{password}
        </if>
        WHERE id = #{id}
    </update>

    <!-- 根据用户名查询 -->
    <select id="findByName" parameterType="String" resultType="com.example.entity.User">
        SELECT * FROM user WHERE username = #{username}
    </select>
</mapper>